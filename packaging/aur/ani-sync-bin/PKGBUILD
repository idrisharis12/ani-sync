# Maintainer: Idris Haris <https://github.com/idrisharis12>
pkgname=ani-sync-bin
pkgver=2.11.25
pkgrel=1
pkgdesc="Stream anime from terminal with 64x turbo speed & auto tracking (Pre-compiled standalone binary)"
arch=('x86_64')
url="https://github.com/idrisharis12/ani-sync"
license=('MIT')
depends=('mpv' 'fzf')
optdepends=(
    'yt-dlp: Recommended for stream acceleration'
    'discord: Discord Rich Presence integration'
)
provides=('ani-sync')
conflicts=('ani-sync')
source_x86_64=("https://github.com/idrisharis12/ani-sync/releases/download/v$pkgver/ani-sync-linux-x86_64")
sha256sums_x86_64=('SKIP')

package() {
    install -Dm755 "$srcdir/ani-sync-linux-x86_64" "$pkgdir/usr/bin/ani-sync"
}
